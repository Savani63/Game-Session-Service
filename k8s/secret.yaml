apiVersion: v1
kind: Secret
metadata:
  name: game-session-secret
  namespace: game-session
type: Opaque
data:
  # WARNING: Hardcoded secrets are for development/testing only!
  # For production, use one of these approaches:
  # 1. Sealed Secrets: https://github.com/bitnami-labs/sealed-secrets
  # 2. External Secrets Operator: https://external-secrets.io/
  # 3. Cloud-native secret managers (AWS Secrets Manager, GCP Secret Manager, Azure Key Vault)
  # 4. Vault: https://www.vaultproject.io/
  # 
  # To generate base64 values: echo -n 'your-password' | base64
  # Base64 encoded values - 'gamepass' and 'rootpass' (CHANGE IN PRODUCTION!)
  DB_PASSWORD: Z2FtZXBhc3M=
  MYSQL_ROOT_PASSWORD: cm9vdHBhc3M=
  MYSQL_PASSWORD: Z2FtZXBhc3M=
